let valor1 = parseInt(gets());
let valor2 = parseInt(gets());
let valor3 = parseFloat(gets());
let salary = parseFloat(valor2 * valor3).toFixed(2); // Digite aqui o calculo do salário
console.log("NUMBER = " + valor1);
console.log("SALARY = U$ " +  salary);


/*  cria a variável nome 
var nome = "Hebin";   //string
var num = 15;         //número
var nabo = ["batata", "biruta", "bolota"]   //array
*/

/* faz aparecer uma alertbox no navegador, 
concatenando o bem vinde com a variável nome 
alert("Bem vinde " + nome);
*/

/* Ao invés de abrir alerts pra testar, inicia o console 
console.log(nome);      //variável dentro dos parenteses para teste
*/

/*  nabo.toLowerCase() -> tudo minusculo
    nabo.toUpperCase() -> tudo maiúsculo    */

/*
    nabo.push("bolinha");   ->  adiciona ao final da lista
    nabo.pop("biruta");     ->  remove do fim da lista ou remove a string dentro do parenteses
    nabo.reverse();         ->  mostra a lista na ordem contrária    
    nabo.toString()         ->  transforma em uma frase
    nabo.join(" - ");       ->  Coloca o que tá dentro do parenteses entre cada um dos elementos do array   
*/
/*          dicionário
    var fruta = {nome:"maçã", cor:"vermelha"}
    fruta.nome              ->  retorna o nome correspondente
    fruta.cor               ->  retorna a cor correspondente
*/

/*      lista de dicionários
    var frutas=[{nome:"maçã", cor:"vermelha"},{nome:"uva", cor:"roxa"}];
    fruta[n].nome           ->  mostra o nome da fruta 'n'
    fruta[n].cor            ->  mostra a cor da fruta 'n'
    */

/*  
    condicionais são feitos iguais ao C
    if, for e while são iguais.
    var idade = prompt("Qual sua idade?");          ->  cria caixa para digitar em uma alertbox
*/

/*      data    
    var d = new Date();
    alert(d.getDate());
    alert(d.getDay()+1);*/